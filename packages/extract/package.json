{
  "name": "@fit-to-figma/extract",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Runs in a browser. Walks the DOM, reads computed style and layout, writes a design tree.",
  "engines": { "node": ">=24" },
  "sideEffects": false,
  "files": ["src"],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fit-to-figma/map": "^0.1.0",
    "@fit-to-figma/tree": "^0.1.0"
  },
  "devDependencies": {
    "fit-to-figma": "^0.1.0",
    "happy-dom": "^20.14.5"
  }
}
